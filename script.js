// 1. Create a string (const)
// 2. Assign variable to 3 combo codes using ariphmetic calculations
// 3. Create an alert showing the string with the vault combo

const message =
  "You have received this message because you have been chosen to open an important vault. Here is the secret combination";

const num1 = 5 + 5;

const num2 = 80 / 2;

const num3 = 40 - 1;

// use all 4 variables above to concatenate a string

alert(`${message}: ${num1} - ${num2} - ${num3}`);
